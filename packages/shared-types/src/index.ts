// Types shared between apps/api and apps/web (request/response DTOs,
// enums mirrored from the Prisma schema, etc.). Populated starting Phase 2
// as auth and domain endpoints are built — kept empty for now so the
// workspace package is wired up ahead of time.
export {};
